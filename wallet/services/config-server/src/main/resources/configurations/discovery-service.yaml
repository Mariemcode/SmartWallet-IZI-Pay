eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false  #c’est lui le serveur Eureka
    fetch-registry: false        #lui qui contient cette liste
    service-url:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka/
server:
  port: 8761
